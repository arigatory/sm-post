---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-read-pvc
  namespace: cqrs-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-read-config
  namespace: cqrs-app
data:
  POSTGRES_USER: "postgres"

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-read-secret
  namespace: cqrs-app
type: Opaque
data:
  POSTGRES_PASSWORD: MTIzNA== # base64 encoded "1234"

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-read
  namespace: cqrs-app
spec:
  serviceName: postgres-read
  replicas: 1 # Stateful service - single instance
  selector:
    matchLabels:
      app: postgres-read
  template:
    metadata:
      labels:
        app: postgres-read
    spec:
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: postgres-read-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-read-secret
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: postgres-read-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: postgres-read-storage
        persistentVolumeClaim:
          claimName: postgres-read-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-read
  namespace: cqrs-app
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres-read
